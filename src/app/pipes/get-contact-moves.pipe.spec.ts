import { GetContactMovesPipe } from './get-contact-moves.pipe';

describe('GetContactMovesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetContactMovesPipe();
    expect(pipe).toBeTruthy();
  });
});
